[package]
name = "cef-renderer"
version = "0.1.0"
edition = "2024"

[dependencies]
cef = { version = "=150.2.1", default-features = false }
image = { version = "0.25.10", default-features = false, features = ["png"] }

