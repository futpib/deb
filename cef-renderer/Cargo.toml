[package]
name = "cef-renderer"
version = "0.1.0"
edition = "2024"

[dependencies]
cef = { version = "=150.2.1", default-features = false }
cef-cookie = { version = "0.1.0", path = "../cef-cookie" }
shell-protocol = { version = "0.1.0", path = "../shell-protocol" }

