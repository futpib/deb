diff --git a/browser/components/moz.build b/browser/components/moz.build
index d1d769ab09..982b571428 100644
--- a/browser/components/moz.build
+++ b/browser/components/moz.build
@@ -41,6 +41,7 @@ DIRS += [
     "downloads",
     "enterprisepolicies",
     "extensions",
+    "firefoxcef",
     "firefoxview",
     "genai",
     "ipprotection",
diff --git a/toolkit/library/libxul.symbols b/toolkit/library/libxul.symbols
index e63e3e7442..902d5ee39f 100644
--- a/toolkit/library/libxul.symbols
+++ b/toolkit/library/libxul.symbols
@@ -1,4 +1,29 @@
 XRE_GetBootstrap
+firefox_cef_gecko_set_callbacks
+firefox_cef_gecko_configure
+firefox_cef_gecko_resize
+firefox_cef_gecko_invalidate
+firefox_cef_gecko_release_frame
+firefox_cef_gecko_browser_for_widget
+firefox_cef_gecko_emit_accelerated_frame
+firefox_cef_gecko_navigate
+firefox_cef_gecko_reload
+firefox_cef_gecko_show_devtools
+firefox_cef_gecko_focus
+firefox_cef_gecko_set_visibility
+firefox_cef_gecko_send_mouse_move
+firefox_cef_gecko_send_mouse_click
+firefox_cef_gecko_send_mouse_wheel
+firefox_cef_gecko_send_touch
+firefox_cef_gecko_send_key
+firefox_cef_gecko_exit_fullscreen
+firefox_cef_gecko_close
+firefox_cef_gecko_shutdown
+firefox_cef_gecko_post_task
+firefox_cef_gecko_visit_cookies
+firefox_cef_gecko_set_cookie
+firefox_cef_gecko_delete_cookie
+firefox_cef_gecko_run
 uprofiler_get
 #ifdef XP_IOS
 MainProcessInit
diff --git a/browser/moz.configure b/browser/moz.configure
--- a/browser/moz.configure
+++ b/browser/moz.configure
@@ -12,6 +12,7 @@
 
 
 imply_option("MOZ_APP_VENDOR", "Mozilla")
+imply_option("MOZ_APP_UA_NAME", "Firefox")
 imply_option("MOZ_APP_ID", "{ec8030f7-c20a-464f-9b0e-13a3a9e97384}")
 # Include the DevTools client, not just the server (which is the default)
 imply_option("MOZ_DEVTOOLS", "all")
diff --git a/gfx/webrender_bindings/RenderCompositor.cpp b/gfx/webrender_bindings/RenderCompositor.cpp
--- a/gfx/webrender_bindings/RenderCompositor.cpp
+++ b/gfx/webrender_bindings/RenderCompositor.cpp
@@ -16,4 +16,5 @@
 #include "mozilla/webrender/RenderCompositorOGL.h"
 #include "mozilla/webrender/RenderCompositorSWGL.h"
 #include "mozilla/widget/CompositorWidget.h"
+#include "prenv.h"
 
@@ -37,6 +38,9 @@
 
 namespace mozilla::wr {
 
+UniquePtr<RenderCompositor> CreateFirefoxCefRenderCompositor(
+    const RefPtr<widget::CompositorWidget>& aWidget, nsACString& aError);
+
 void wr_compositor_add_surface(void* aCompositor, wr::NativeSurfaceId aId,
                                const wr::CompositorSurfaceTransform* aTransform,
                                wr::DeviceIntRect aClipRect,
@@ -200,6 +204,11 @@
 /* static */
 UniquePtr<RenderCompositor> RenderCompositor::Create(
     const RefPtr<widget::CompositorWidget>& aWidget, nsACString& aError) {
+  if (PR_GetEnv("FIREFOX_CEF_DIRECT") && aWidget->AsDelegate() &&
+      aWidget->AsDelegate()->AsHeadlessCompositorWidget()) {
+    return CreateFirefoxCefRenderCompositor(aWidget, aError);
+  }
+
   if (aWidget->GetCompositorOptions().UseSoftwareWebRender()) {
 #ifdef XP_DARWIN
     // Mac uses NativeLayerCA
diff --git a/xpfe/appshell/AppWindow.cpp b/xpfe/appshell/AppWindow.cpp
--- a/xpfe/appshell/AppWindow.cpp
+++ b/xpfe/appshell/AppWindow.cpp
@@ -95,6 +95,8 @@
 //*****************************************************************************
 
 namespace mozilla {
+
+extern "C" int firefox_cef_gecko_take_devtools_window();
 
 using dom::AutoNoJSAPI;
 using dom::BrowserHost;
@@ -184,7 +186,11 @@
                           DesktopIntSize(aInitialWidth, aInitialHeight));
 
   // Create top level window
-  if (gfxPlatform::IsHeadless()) {
+  const bool firefoxCefDirect = PR_GetEnv("FIREFOX_CEF_DIRECT");
+  const bool firefoxCefDevTools =
+      firefoxCefDirect && firefox_cef_gecko_take_devtools_window();
+  if (gfxPlatform::IsHeadless() ||
+      (firefoxCefDirect && !firefoxCefDevTools)) {
     mWindow = nsIWidget::CreateHeadlessWidget();
   } else {
     mWindow = nsIWidget::CreateTopLevelWindow();
diff --git a/widget/headless/HeadlessWidget.cpp b/widget/headless/HeadlessWidget.cpp
--- a/widget/headless/HeadlessWidget.cpp
+++ b/widget/headless/HeadlessWidget.cpp
@@ -22,6 +22,9 @@
 #include "nsIScreen.h"
 #include "HeadlessKeyBindings.h"
 
+extern "C" void firefox_cef_gecko_cursor_changed(nsIWidget *aWidget,
+                                                   uint32_t aCursor);
+
 using namespace mozilla;
 using namespace mozilla::gfx;
 using namespace mozilla::layers;
@@ -130,6 +133,11 @@ void HeadlessWidget::GetCompositorWidgetInitData(
       mozilla::widget::HeadlessCompositorWidgetInitData(GetClientSize());
 }
 
+void HeadlessWidget::SetCursor(const Cursor& aCursor) {
+  nsIWidget::SetCursor(aCursor);
+  firefox_cef_gecko_cursor_changed(this, aCursor.mDefaultCursor);
+}
+
 void HeadlessWidget::RaiseWindow() {
   MOZ_ASSERT(
       mWindowType == WindowType::TopLevel || mWindowType == WindowType::Dialog,
diff --git a/widget/headless/HeadlessWidget.h b/widget/headless/HeadlessWidget.h
--- a/widget/headless/HeadlessWidget.h
+++ b/widget/headless/HeadlessWidget.h
@@ -57,6 +57,7 @@ class HeadlessWidget final : public nsIWidget {
       mozilla::widget::CompositorWidgetInitData* aInitData) override;
 
   void Destroy() override;
+  void SetCursor(const Cursor& aCursor) override;
   void Show(bool aState) override;
   bool IsVisible() const override;
   void Move(const DesktopPoint&) override;
